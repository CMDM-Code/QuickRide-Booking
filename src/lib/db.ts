/**
 * db.ts — Stub (SQLite removed per A1)
 * All data is stored in Firebase Firestore only.
 * This file is kept as an empty module to avoid broken imports.
 */

export const dbService = {
  // No-op stub. All auth/data goes through Firebase Auth + Firestore.
};
